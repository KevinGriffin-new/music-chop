<?xml version="1.0" encoding="UTF-8"?>
<!--
  Hardened-runtime entitlements for the notarized dv2mv.app.

  numba (pulled in by librosa during the Analyze stage) JIT-compiles to
  executable memory at runtime; under the hardened runtime that requires the
  allow-jit / unsigned-executable-memory entitlements. disable-library-validation
  lets the app load the bundled, third-party-signed ffmpeg/ffprobe/rubberband
  dylibs that aren't signed by our Team ID.
-->
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <key>com.apple.security.cs.allow-jit</key>
    <true/>
    <key>com.apple.security.cs.allow-unsigned-executable-memory</key>
    <true/>
    <key>com.apple.security.cs.disable-library-validation</key>
    <true/>
</dict>
</plist>
